version: '3.8'

services:
  jarvis-app:
    build: .
    container_name: jarvis_ai_copilot
    restart: always
    ports:
      - "8501:8501"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./wiki:/app/wiki
      - ./raw:/app/raw
      - ./.env:/app/.env
